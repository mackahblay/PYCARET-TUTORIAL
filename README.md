# PYCARET-TUTORIAL
he pycaret library in Python makes it very easier to automate model trainings. It trains multiple models (logistic models, naive bayes, ridge classfieir, ada boost, svm, k neighbors, gradient boosting, etc). It is efficiently and saves project time
